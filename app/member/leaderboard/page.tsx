import { Topline } from "@/components/ui/Topline"; import { PageIntro } from "@/components/cards/PageIntro"; import { mockRepository } from "@/lib/services";
export default function Leaderboard() { return <><Topline section="A little friendly pressure" /><PageIntro kicker="Season one" title="People in motion." description="Progress is more fun when you can see the shape of everyone else's journey." /><div className="list">{mockRepository.getLeaders().map(leader => <div className="list-item" key={leader.rank}><div style={{ display: "flex", gap: 22, alignItems: "center" }}><span className="eyebrow">0{leader.rank}</span><div><strong>{leader.name}</strong><div className="muted">{leader.handle}</div></div></div><strong>{leader.points.toLocaleString()} pts</strong></div>)}</div></>; }
