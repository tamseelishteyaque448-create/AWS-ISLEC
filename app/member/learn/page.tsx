import { Topline } from "@/components/ui/Topline"; import { PageIntro } from "@/components/cards/PageIntro"; import { mockRepository } from "@/lib/services";
export default function Learn() { return <><Topline section="Learn / practice / repeat" /><PageIntro kicker="Your curriculum" title="Get better by shipping." description="Short, practical paths for the moments when you want to understand a thing well enough to use it." /><div className="grid">{mockRepository.getChallenges().map(challenge => <article className="panel" key={challenge.title}><span className="tag">{challenge.level}</span><h2 style={{ marginTop: 18 }}>{challenge.title}</h2><p className="muted">{challenge.detail}</p><div className="eyebrow" style={{ marginTop: 30 }}>+{challenge.points} pts</div></article>)}</div></>; }
