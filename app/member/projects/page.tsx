import { Topline } from "@/components/ui/Topline"; import { PageIntro } from "@/components/cards/PageIntro"; import { mockRepository } from "@/lib/services";
export default function Projects() { return <><Topline section="Build / document / share" /><PageIntro kicker="Project studio" title="See projects in motion." description="Browse community examples while the member project workspace is being connected." /><div className="list">{mockRepository.getProjects().map(project => <div className="list-item" key={project.title}><div><strong>{project.title}</strong><div className="muted">{project.category}</div></div><div style={{ width: 150 }}><div className="muted" style={{ marginBottom: 6 }}>{project.progress}%</div><div className="progress-track"><div className="progress-value" style={{ width: `${project.progress}%` }} /></div></div></div>)}</div></>; }
