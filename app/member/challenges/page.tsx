import { PageIntro } from "@/components/cards/PageIntro"; import { Topline } from "@/components/ui/Topline"; import { mockRepository } from "@/lib/services";
export default function Challenges() { return <><Topline section="Learn / practice / repeat" /><PageIntro kicker="Practical prompts" title="Choose your next stretch." description="Small, concrete challenges that turn cloud knowledge into something you can use." /><div className="grid">{mockRepository.getChallenges().map(challenge => <article className="panel" key={challenge.title}><span className="tag">{challenge.level}</span><h2 style={{ marginTop: 18 }}>{challenge.title}</h2><p className="muted">{challenge.detail}</p><div className="eyebrow" style={{ marginTop: 30 }}>+{challenge.points} pts</div></article>)}</div></>; }
