import { Topline } from "@/components/ui/Topline"; import { PageIntro } from "@/components/cards/PageIntro";
export default function Journey() { return <><Topline section="The long way around" /><PageIntro kicker="Your journey" title="Small steps count." description="A living timeline of lessons, builds, and the people who helped you keep moving." /><div className="list">{["Joined the ISLEC community", "Completed Open book badge", "Started Signal Garden", "Shared your first project"].map((item, index) => <div className="list-item" key={item}><div><span className="eyebrow">0{index + 1}</span> <strong style={{ marginLeft: 16 }}>{item}</strong></div><span className="muted">{index < 2 ? "completed" : "in progress"}</span></div>)}</div></>; }
