import { Topline } from "@/components/ui/Topline"; import { PageIntro } from "@/components/cards/PageIntro"; import { mockRepository } from "@/lib/services";
export default function Explore() { return <><Topline section="Explore the community" /><PageIntro kicker="People / projects / ideas" title="See what is being built." description="Follow the threads, projects, and curious experiments moving through the community right now." /><div className="list">{mockRepository.getProjects().map(project => <div className="list-item" key={project.title}><div><strong>{project.title}</strong><div className="muted">{project.category} / {project.status}</div></div><span className="tag">{project.progress}% complete</span></div>)}</div></>; }
