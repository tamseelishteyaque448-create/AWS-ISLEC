import { Topline } from "@/components/ui/Topline"; import { PageIntro } from "@/components/cards/PageIntro"; import { currentUser } from "@/data/users"; import { badges } from "@/data/badges";
export default function Profile() { return <><Topline section="A record of becoming" /><PageIntro kicker={currentUser.handle} title={currentUser.name} description={`${currentUser.role}. Learning out loud, one small deployment at a time.`} /><div className="grid"><div className="panel"><h2>Points</h2><div className="metric">{currentUser.points.toLocaleString()}</div></div><div className="panel"><h2>Streak</h2><div className="metric">{currentUser.streak}<span style={{ color: "var(--aws)" }}>d</span></div></div><div className="panel"><h2>Badges</h2><div className="list">{badges.map(badge => <div className="list-item" key={badge.title}><span>{badge.icon} {badge.title}</span><span className="tag">{badge.earned ? "Earned" : "Locked"}</span></div>)}</div></div></div></>; }
