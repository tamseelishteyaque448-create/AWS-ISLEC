import { Topline } from "@/components/ui/Topline"; import { PageIntro } from "@/components/cards/PageIntro"; import { mockRepository } from "@/lib/services";
export default function Events() { return <><Topline section="Make room for serendipity" /><PageIntro kicker="Gatherings" title="Come as you are." description="A calendar of workshops, study halls, and moments to share the thing you just figured out." /><div className="list">{mockRepository.getEvents().map(event => <div className="list-item" key={event.title}><div style={{ display: "flex", gap: 24, alignItems: "center" }}><span className="eyebrow">{event.date}</span><div><strong>{event.title}</strong><div className="muted">{event.type} / {event.time}</div></div></div><span className="tag">Join</span></div>)}</div></>; }
