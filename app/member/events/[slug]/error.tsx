"use client";

import { EventDetailError } from "@/components/events/EventDetailError";

export default function MemberEventDetailError({ reset }: { reset: () => void }) { return <EventDetailError reset={reset} />; }
